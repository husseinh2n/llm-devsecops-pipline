name: Security Scan

on:
  push:
    branches: ["main"]
  pull_request:
    branches: [main]

# Grant the workflow write access to contents and pull-requests
permissions:
  contents: write
  pull-requests: write

jobs:
  # ──────────────────────────────────────────────────────────────
  #  Job 1: Static Analysis with Semgrep
  # ──────────────────────────────────────────────────────────────
  semgrep:
    name: Semgrep SAST
    runs-on: ubuntu-latest
    container:
      image: semgrep/semgrep:latest
    outputs:
      has_findings: ${{ steps.check.outputs.has_findings }}

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Run Semgrep
        run: |
          semgrep \
            --config .semgrep/rules.yml \
            --config "p/python" \
            --json \
            --output semgrep-results.json \
            . || true

      - name: Upload Semgrep results
        uses: actions/upload-artifact@v4
        with:
          name: semgrep-results
          path: semgrep-results.json

      - name: Check for critical findings
        id: check
        run: |
          python3 - <<'EOF'
          import json, os
          with open("semgrep-results.json") as f:
              data = json.load(f)
          findings = data.get("results", [])
          critical = [r for r in findings if r.get("extra", {}).get("severity") == "ERROR"]
          print(f"Total findings : {len(findings)}")
          print(f"Critical (ERROR): {len(critical)}")
          for c in critical:
              print(f"  [{c['check_id']}] {c['path']}:{c['start']['line']} — {c['extra']['message']}")
          if critical:
              print("\n⚠️  Critical findings detected — AI remediation will be triggered.")
              with open(os.environ["GITHUB_OUTPUT"], "a") as out:
                  out.write("has_findings=true\n")
          else:
              print("✅  No critical findings.")
              with open(os.environ["GITHUB_OUTPUT"], "a") as out:
                  out.write("has_findings=false\n")
          EOF

  # ──────────────────────────────────────────────────────────────
  #  Job 2: Secret Scanning with TruffleHog
  # ──────────────────────────────────────────────────────────────
  trufflehog:
    name: TruffleHog Secret Scan
    runs-on: ubuntu-latest
    outputs:
      has_findings: ${{ steps.check.outputs.has_findings }}

    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0          # full history so TruffleHog can diff

      - name: Run TruffleHog
        id: trufflehog
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --json --only-verified
        continue-on-error: true

      - name: Save TruffleHog output
        run: |
          trufflehog filesystem . \
            --json \
            --no-update \
            2>/dev/null | head -1000 > trufflehog-results.json || true
          echo "TruffleHog scan complete."

      - name: Upload TruffleHog results
        uses: actions/upload-artifact@v4
        with:
          name: trufflehog-results
          path: trufflehog-results.json

      - name: Check for secrets
        id: check
        run: |
          if [ -s trufflehog-results.json ]; then
            echo "⚠️  Secrets detected — AI remediation will be triggered."
            cat trufflehog-results.json
            echo "has_findings=true" >> "$GITHUB_OUTPUT"
          else
            echo "✅  No verified secrets found."
            echo "has_findings=false" >> "$GITHUB_OUTPUT"
          fi

  # ──────────────────────────────────────────────────────────────
  #  Job 3: AI Remediation PR
  #  Runs only when at least one scan job found issues (push to main)
  # ──────────────────────────────────────────────────────────────
  ai-remediation:
    name: AI Auto-Remediation PR
    runs-on: ubuntu-latest
    needs: [semgrep, trufflehog]
    # Run only on push to main AND when at least one scanner found something
    if: |
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main' &&
      (needs.semgrep.outputs.has_findings == 'true' || needs.trufflehog.outputs.has_findings == 'true')

    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT }}
          fetch-depth: 0

      - name: Configure git to use PAT for push
        run: |
          git remote set-url origin https://x-access-token:${{ secrets.GH_PAT }}@github.com/${{ github.repository }}.git
          git config user.name  "ai-security-bot"
          git config user.email "ai-security-bot@noreply.github.com"

      - name: Download Semgrep results
        uses: actions/download-artifact@v4
        with:
          name: semgrep-results
          path: .          # download into the workspace root so the script finds it
        continue-on-error: true

      - name: Download TruffleHog results
        uses: actions/download-artifact@v4
        with:
          name: trufflehog-results
          path: .          # same here
        continue-on-error: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install google-generativeai requests

      - name: Run AI remediation
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GH_TOKEN:       ${{ secrets.GH_PAT }}
          GH_REPO:        ${{ github.repository }}
          BASE_BRANCH:    ${{ github.ref_name }}
        run: python scripts/ai_remediate.py

      - name: Summary
        run: echo "AI remediation complete — check open PRs for auto-generated fixes."
